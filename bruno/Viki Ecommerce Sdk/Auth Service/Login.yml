info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/v1/login
  body:
    type: json
    data: |-
      {
        "emailId": "imviki@sdk.com",
        "password": "imviki@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
