opencollection: 1.0.0

info:
  name: Viki Ecommerce Sdk
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{AUTH_TOKEN}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
